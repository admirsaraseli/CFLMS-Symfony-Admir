<form method="post" action="{{ path('classic_cars_delete', {'clsCarsId': classic_car.clsCarsId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classic_car.clsCarsId) }}">
    <button class="btn btn-danger w-100 mt-2">Delete</button>
</form>
